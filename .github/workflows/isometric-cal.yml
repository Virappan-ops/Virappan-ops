name: Generate Isometric Contributions Graph

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

# ISKO ADD KIYA HAI: Repository me push karne ke liye right permissions dena
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Node 24 support ke liye v4 par update kiya
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Node 24 support ke liye latest v0.8.0 par update kiya
      - name: Generate 3D City
        uses: yoshi389111/github-profile-3d-contrib@0.8.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}

      - name: Commit & Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A .
          git diff-index --quiet HEAD || git commit -m "Generated 3D Isometric Calendar"
          git push
